# REST_API+GraphQL

A application for a REST API and GraphQL server running on express.js

1. run `npm install` to install the dependencies
2. run `npm start` to start the application.
3. the front-end is a stand alone react app in `/react_frontend` folder
4. run `npm install` and `npm start` on the frontend app to start a react dev server for the application.
